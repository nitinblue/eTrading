UNDEFINED_RISK:
  PORTFOLIO_CAP:
    MAX_RISK_DOLLARS: 20_000

  TRADE_LIMITS:
    MAX_RISK_PCT_OF_CAPITAL: 5.0
    MAX_RISK_DOLLARS: 5_000

  COMMON_EXIT_RULES:
    MAX_LOSS_MULTIPLE: 2.0               # 2x credit
    FORCE_EXIT_DTE: 21

  STRATEGIES:
    CSP:
      ENABLED: true
      DELTA_TARGET: -0.20
      MIN_DAYS_TO_EXPIRY: 30
      MAX_ASSIGNMENT_COUNT: 1

      MODELED_RISK:
        STOP_PRICE_MULTIPLIER: 1.5        # strike – (1.5 * ATR)
        USE_ATR: true
        ATR_LOOKBACK: 14

      ADJUSTMENTS:
        ROLL_ALLOWED: true
        MAX_ROLLS: 1

    COVERED_CALL:
      ENABLED: true
      DELTA_TARGET: 0.20
