general:
  use_mock: true
  dry_run: true

broker:
  username: "your@email.com"
  password: "your_password"
  environment: "paper"  # or "live"
  default_account_id: "123456789"  # Optional: specify primary account

risk:
  max_risk_per_trade: 0.01
  max_portfolio_risk: 0.05

strategies:
  short_put:
    min_iv: 30
    max_delta: 0.25
    target_profit: 60
    max_loss: 120
    dry_run: true